import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'netflix-clone-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
